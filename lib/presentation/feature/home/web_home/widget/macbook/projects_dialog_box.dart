import 'package:flutter/material.dart';

class ProjectsDialogBox extends StatelessWidget {
  const ProjectsDialogBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
